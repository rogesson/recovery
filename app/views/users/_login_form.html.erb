Already have account?
<%= form_tag(@session) do %>
  <%= text_field_tag :login, nil, placeholder: 'Login', required: true %>
  <%= password_field_tag :password, nil, placeholder: 'Password', required: true %>
  <%= submit_tag 'Sign in' %>
  
  <% if flash[:bad_login] %>
    Wrong login or password, please try again.</b>
  <% end %>
<% end %>