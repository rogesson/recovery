<%= form_tag(credentials_path, method: :get) do %>
	<%= select_tag :column, options_for_select([ ['Search', '0'], ['Site', 'site'], ['Login', 'login'] ]) %>

	<%= text_field_tag :term, nil, placeholder: 'Search' %>

	<%= submit_tag 'Search', :class => 'Button' %>
<% end %>