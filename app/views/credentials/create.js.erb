if('<%= @response %>' == "success"){
  $.colorbox.close() 
  appendHTML();
} else {
  alert('Error');
}

function appendHTML(){
	console.log('bateu aqui')
	$("<%= j render(partial: 'edit_form', credential: @new_credential) %>").insertAfter("tr:eq(0)");
}