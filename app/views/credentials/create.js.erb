if('<%= @response %>' == "success"){
  $("#cboxOverlay").click();
  appendHTML();
} else {
  alert('Error');
}

function appendHTML(){
	$("<%= j render(partial: 'edit_form', credential: @new_credential) %>").insertAfter( "tr" );
}